# PACMAN Game Center — сайт сети клубов (Актобе)

Сайт шести компьютерных клубов Pacman: клубы, залы, железо, прайс, акции и бронь через WhatsApp.

## Запуск

```bash
npm install
npm run dev      # http://localhost:4321
npm run build    # статическая сборка в dist/
npm test         # тесты текста брони и целостности данных
```

## Превью на GitHub Pages

Каждый пуш в `main` собирает сайт и публикует его по адресу
https://mumitrol16666-creator.github.io/packman/ (workflow `.github/workflows/preview.yml`, ветка `gh-pages`).
Превью закрыто от поисковиков метатегом `noindex`. Собрать такую же версию локально: `npm run build:preview`.

Боевая сборка (`npm run build`) рассчитана на корень домена `pacman.kz`. Другой домен или подпапка задаются
переменными `SITE_URL` и `SITE_BASE`. Все внутренние ссылки строятся через `url()` из `src/lib/url.js`.

## Объём первой версии

Информация о клубах, прайс, железо, акции и бронь через WhatsApp. Онлайн-загрузку залов и интеграцию
с клубной программой в первой версии не делаем. Дальше по плану: админка для маркетологов,
упаковка в Docker и выкладка на сервер, раздел турниров, казахская версия.

## Где что менять

| Что | Файл |
| --- | --- |
| Клубы: адрес, WhatsApp, зоны, железо, цены | `src/data/branches.json` |
| Акции, удобства, правила, время пакетов | `src/data/site.json` |
| Фото клуба | `src/assets/photos/<slug>/` — подхватываются автоматически, первое по имени становится обложкой |
| Картинки для превью ссылок | `npm run og` пересобирает `public/og/` из обложек клубов |
| Текст сообщения в WhatsApp | `src/lib/booking.js` |
| Цвета и шрифты | `src/styles/global.css` |

У клуба без фотографий (сейчас Prime и City) вместо обложки рисуется заглушка с названием. Достаточно положить снимки в папку клуба.

## Бронь

Форма собирает сообщение и открывает `wa.me` нужного клуба. Сайт сам ничего не отправляет и не хранит имён и телефонов. В конце сообщения стоит код заявки (`#A7K2`), по которому администраторы могут сверять заявки с сайта.

## Аналитика и отчёты в Telegram

Сервис лежит в `server/`, зависимостей у него нет: нужен только Node 22.12+ (используется встроенный SQLite).

```bash
npm run report:demo   # три отчёта на тестовых данных: так они будут выглядеть в Telegram
npm run server        # приём событий на :8787 и рассылка отчётов
```

Как это работает:

1. Сайт собирается с `PUBLIC_TRACK_URL=https://<домен>/api/track` и шлёт события: просмотры, разделы, клики
   по кнопкам (`data-track`), заявки на бронь с кодом, время на сайте. Без переменной сайт ничего не отправляет.
2. Сервис пишет события в SQLite (`DB_PATH`). IP-адреса не сохраняются: посетитель считается по хэшу,
   который меняется каждый день. Боты и мусорные запросы отбрасываются.
3. В `REPORT_HOUR` по времени клуба бот отправляет отчёт за вчера, по понедельникам — итоги недели,
   первого числа — итоги месяца. Отправленное запоминается, после простоя сервер не шлёт старые отчёты.

Переменные окружения описаны в `.env.example`. Бота создаёт @BotFather; бота нужно добавить администратором
в канал отчётов, `TG_REPORT_CHAT_ID` — это `@имя_канала` или числовой id вида `-100…`.
Если задать `TG_BOOKINGS_CHAT_ID`, в этот чат будет приходить уведомление о каждой заявке с кодом.

Отчёт вручную: `node server/cli.js report daily --date=2026-09-18 --send` (также `weekly`, `monthly`).

Свои посещения исключаются ссылкой `https://<домен>/?internal=1` (вернуть обратно — `?internal=0`):
откройте её один раз на компьютерах администраторов.

Яндекс Метрика подключается переменной `PUBLIC_METRIKA_ID` при сборке сайта; клики уходят в неё как цели.

## Источники данных

Адреса, телефоны, железо и цены взяты из карточек клубов в 2ГИС (обновлены 08.09.2026) и сверены со сторис из брендбука. Расхождения, которые нужно подтвердить у клуба, записаны в поле `needsReview` в `branches.json`.
